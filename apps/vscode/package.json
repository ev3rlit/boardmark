{
  "name": "@boardmark/vscode",
  "displayName": "Boardmark",
  "description": "Open Boardmark markdown documents in the shared canvas editor.",
  "private": true,
  "version": "0.0.1",
  "publisher": "boardmark",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Visualization", "Other"],
  "main": "./dist/extension/index.js",
  "scripts": {
    "build:webview": "vite build --config vite.config.webview.ts",
    "build:extension": "vite build --config vite.config.extension.ts",
    "build": "pnpm run build:webview && pnpm run build:extension",
    "watch:webview": "vite build --watch --config vite.config.webview.ts",
    "watch:extension": "vite build --watch --config vite.config.extension.ts",
    "watch": "node scripts/watch.mjs",
    "package": "vsce package --no-dependencies"
  },
  "activationEvents": [
    "onCommand:boardmark.openAsCanvas",
    "onCustomEditor:boardmark.canvasEditor"
  ],
  "contributes": {
    "customEditors": [
      {
        "viewType": "boardmark.canvasEditor",
        "displayName": "Boardmark Canvas",
        "selector": [
          { "filenamePattern": "*.md" }
        ],
        "priority": "option"
      }
    ],
    "commands": [
      {
        "command": "boardmark.openAsCanvas",
        "title": "Boardmark: Open as Canvas"
      },
      {
        "command": "boardmark.openAsText",
        "title": "Boardmark: Open as Text"
      }
    ]
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.0"
  }
}
